<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Светлая тема, Android 12+ (API 31): системный SplashScreen API.

        windowSplashScreenAnimatedIcon = @drawable/splash_icon —
        AVD с GitHub-логотипом (accent #9885E2). Анимация: только scale
        0.85→1.0 за 450ms. fillAlpha НЕ анимируется (был убран) — это
        фикс бага «маленькая иконка → большая»: раньше первый кадр был
        пустым (alpha=0), часть OEM-ROM-ов показывала ic_launcher-fallback.
        Теперь иконка видна с кадра 0, плавно «надувается» в scale.
    -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:windowTranslucentNavigation">false</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowSplashScreenBackground">#FFFFFF</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="android:windowSplashScreenAnimationDuration">@integer/splash_screen_duration</item>
    </style>
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:windowTranslucentNavigation">false</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>
